# Backend origin, no trailing slash and no /api suffix (e.g. https://ecommerce-backend.onrender.com).
# Leave empty for local dev — Vite's dev server proxies /api and /socket.io to
# http://localhost:5000 instead (see vite.config.js).
VITE_API_URL=
